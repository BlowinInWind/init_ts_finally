/** @format */

import React from 'react';

const Index: React.FC = (props: any) => {
    return (
        <>
            {props.children}
            <br />1
        </>
    );
};

export default Index;
